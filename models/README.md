# Prize models

Higgsfield-generated (generate_image → generate_3d) low-poly GLB props for the
claw-machine Hero: ring (Wedding), cake (Birthday), gamepad (Games),
briefcase (Business). Regenerate via the Higgsfield MCP; keep each ≤ ~2 MB.
